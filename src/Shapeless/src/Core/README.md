## 关于 Core 目录的单元测试覆盖率

`Core` 文件夹是从 `Furion` 框架核心代码中抽离出来、供 `Shapeless` 使用的功能模块。因此，`Core` 的完整单元测试仍保留在
`Furion` 框架仓库中，此处未再拷贝，以避免维护多套重复的测试代码。

在使用单元测试工具统计覆盖率时，整体覆盖率可能低于 **98%**。原因在于 `Core` 目录的单元测试并未包含在当前工程中，这部分测试代码位于
`Furion` 框架，属于 `Furion` 生态的公共部分。